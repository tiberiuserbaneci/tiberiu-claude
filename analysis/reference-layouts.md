# Reference Layout Library — extracted from the 72 uploaded materials

> Built 2026-06 by rendering and studying every reference poster in `content/`
> (script-01..55 + docs-01/02). Purpose: STOP defaulting to one card-grid template.
> Each new material must pick a DIFFERENT archetype from this menu and rebuild it
> in the Dark Ultron brand (DM Sans/Mono + slate/book), never the source fonts.
>
> Rule (per operator, 2026-06): do not build from my own trained-in layouts.
> Build from these reference structures. Never repeat an archetype on consecutive posts.

## The genres in the references
- **PROOF / RESULT** (most scripts): lead with a number + back it with ONE real data viz.
- **LIGHT EDITORIAL**: cream/white background variants (not everything is dark slate).
- **TERMINAL / SYSTEM**: monospace, log/code aesthetic.

## The archetypes (the menu)

| # | Archetype | What it is | Example refs |
|---|-----------|------------|--------------|
| A1 | **Bar-chart hero** | Title + dominant vertical bar chart, supporting rows below | script-30 "1,847 ops" |
| A2 | **Giant-number ledger** | One huge number (56 / 221x / 412 / 01) owns the page, a thin ledger/list beside it | script-43 "221x", script-29 "412" |
| A3 | **Data table / spreadsheet** | A real table: header row, columns, totals — reads like a sheet | script-31/32 "5 pricing pages", script-40 "12,400 accounts" |
| A4 | **Line / decay curve** | A single line or area chart showing a trend, decay or flatline | script-33/34 "flatlines at minute 4" |
| A5 | **Dense dashboard** | Multiple panels (mini-charts + stat tiles + a table) like an analytics screen | script-35/36/37 "audited", "14 competitors" |
| A6 | **Leaderboard with bars** | Ranked rows, each a horizontal bar + value | script-38 "348 inbound leads" |
| A7 | **Flagged row list** | A list of items/accounts with status chips, like a queue | script-39 "23 accounts" |
| A8 | **Scatter plot** | Dots on X/Y axes, a few highlighted | script-40 "847 picked" |
| A9 | **Funnel** | Stages narrowing top to bottom, drop-off labelled | script-41 cold-email funnel |
| A10 | **Calendar / heatmap grid** | A grid of colored cells (days, contribution-style) | script-42 "36 days", the red/green grid |
| A11 | **You-vs-AI split** | Two columns contrasting the manual way vs the AI way | script-44/45 "7 emails vs 1" |
| A12 | **Radial / orbit** | A central node with orbiting items, or a circular hit map | "discovery hits 7 of 7" |
| A13 | **Terminal / command log** | Monospace console: prompts, slash commands, log lines, syntax color | the "Cloned my SDR" set |
| A14 | **Big ratio / time** | A dominant ratio or time (3:11, 1.61 days, 10x) as the hook | script "3:11", "1.61 days" |

## Brand rebuild rules (applies to every archetype above)
- Fonts: DM Sans + DM Mono only (sources use Inter/Fraunces/JetBrains — never copy those).
- Palette: Dark Ultron slate/book/kraft, OR the light REALNUMBERS cream for the light-editorial ones.
- Keep: §11 footer, the CTA keyword chip, 1080×1450 (LinkedIn) / 1080×1920 (TikTok).
- Charts: real, via ECharts (inlined) or hand SVG. Icons: Iconify (Lucide/Phosphor/Simple-Icons), inlined.

## Process going forward (less chaos)
1. For each material, pick ONE archetype from the menu (operator chooses, or I recommend).
2. Rebuild that exact STRUCTURE with the new content, in Dark Ultron / REALNUMBERS.
3. Track which archetype each material used; never reuse on the next one.

## Archetype usage log
- docs-03 chat — (my default 3-col card grid — NOT from this menu — to be reworked)
- docs-04 workforce — A5 dense dashboard, CLONED from script-37, REALNUMBERS light, DM Sans/Mono. Max-density pass: stat-tiles band + 7-col roster (tier/pairs/returns) + flow strip. Roster = 7 agents (CLAUDE.md §1 aligned 2026-06-04).
- docs-04 workforce TikTok infographic — A12 radial/orbit (router hub + 7 agent nodes + SVG wires), Dark Ultron glass. Replaces the card-grid version (too similar to last 10 posts). Carousel (8 slides) unchanged.
