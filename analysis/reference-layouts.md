# Reference Layout Library — extracted from the 72 uploaded materials

> Built 2026-06 by rendering and studying every reference poster in `content/`
> (script-01..55 + docs-01/02). Purpose: STOP defaulting to one card-grid template.
> Each new material must pick a DIFFERENT archetype from this menu and rebuild it
> in the Dark Ultron brand (DM Sans/Mono + slate/book), never the source fonts.
>
> Rule (per operator, 2026-06): do not build from my own trained-in layouts.
> Build from these reference structures. Never repeat an archetype on consecutive posts.

## The genres in the references
- **PROOF / RESULT** (most scripts): lead with a number + back it with ONE real data viz.
- **LIGHT EDITORIAL**: cream/white background variants (not everything is dark slate).
- **TERMINAL / SYSTEM**: monospace, log/code aesthetic.

## The archetypes (the menu)

| # | Archetype | What it is | Example refs |
|---|-----------|------------|--------------|
| A1 | **Bar-chart hero** | Title + dominant vertical bar chart, supporting rows below | script-30 "1,847 ops" |
| A2 | **Giant-number ledger** | One huge number (56 / 221x / 412 / 01) owns the page, a thin ledger/list beside it | script-43 "221x", script-29 "412" |
| A3 | **Data table / spreadsheet** | A real table: header row, columns, totals — reads like a sheet | script-31/32 "5 pricing pages", script-40 "12,400 accounts" |
| A4 | **Line / decay curve** | A single line or area chart showing a trend, decay or flatline | script-33/34 "flatlines at minute 4" |
| A5 | **Dense dashboard** | Multiple panels (mini-charts + stat tiles + a table) like an analytics screen | script-35/36/37 "audited", "14 competitors" |
| A6 | **Leaderboard with bars** | Ranked rows, each a horizontal bar + value | script-38 "348 inbound leads" |
| A7 | **Flagged row list** | A list of items/accounts with status chips, like a queue | script-39 "23 accounts" |
| A8 | **Scatter plot** | Dots on X/Y axes, a few highlighted | script-40 "847 picked" |
| A9 | **Funnel** | Stages narrowing top to bottom, drop-off labelled | script-41 cold-email funnel |
| A10 | **Calendar / heatmap grid** | A grid of colored cells (days, contribution-style) | script-42 "36 days", the red/green grid |
| A11 | **You-vs-AI split** | Two columns contrasting the manual way vs the AI way | script-44/45 "7 emails vs 1" |
| A12 | **Radial / orbit** | A central node with orbiting items, or a circular hit map | "discovery hits 7 of 7" |
| A13 | **Terminal / command log** | Monospace console: prompts, slash commands, log lines, syntax color | the "Cloned my SDR" set |
| A14 | **Big ratio / time** | A dominant ratio or time (3:11, 1.61 days, 10x) as the hook | script "3:11", "1.61 days" |

## Brand rebuild rules (applies to every archetype above)
- Fonts: DM Sans + DM Mono only (sources use Inter/Fraunces/JetBrains — never copy those).
- Palette: Dark Ultron slate/book/kraft, OR the light REALNUMBERS cream for the light-editorial ones.
- Keep: §11 footer, the CTA keyword chip, 1080×1450 (LinkedIn) / 1080×1920 (TikTok).
- Charts: real, via ECharts (inlined) or hand SVG. Icons: Iconify (Lucide/Phosphor/Simple-Icons), inlined.

## Process going forward (less chaos)
1. For each material, pick ONE archetype from the menu (operator chooses, or I recommend).
2. Rebuild that exact STRUCTURE with the new content, in Dark Ultron / REALNUMBERS.
3. Track which archetype each material used; never reuse on the next one.

## Archetype usage log
- docs-03 chat — (my default 3-col card grid — NOT from this menu — to be reworked)
- docs-04 workforce — A5 dense dashboard, CLONED from script-37, REALNUMBERS light, DM Sans/Mono. Max-density pass: stat-tiles band + 7-col roster (tier/pairs/returns) + flow strip. Roster = 7 agents (CLAUDE.md §1 aligned 2026-06-04).
- docs-04 workforce TikTok infographic — A12 radial/orbit (router hub + 7 agent nodes + SVG wires), Dark Ultron glass. Replaces the card-grid version (too similar to last 10 posts). Carousel (8 slides) unchanged.
- docs-05 Background Jobs LinkedIn — DIRECTED JOURNEY (not table): entry band > focal running-job card with live streaming console > 4-way outcome branch > artifact band, connected by labelled arrows. Fixes flat/airy/tabular: one dominant focal element, a visual path, dense annotation. Supersedes the flat panel-mock.

## DENSITY STANDARD (extracted from script-23, the operator's approved bar) 2026-06-04
Operator rejected list/table/panel layouts as flat/airy/tabular. The real "dense + save-worthy"
standard = a STACK OF ~6 DIFFERENT MODULES, each packed with REAL NUMBERS, with variety:
1. mast (keywords)  2. hook (ACCENT word + number, 2nd line muted) + mono sub-line of numbers
3. hero strip: solid book bar, 4 big stats  4. a 2x2/3up grid of cards EACH WITH A MINI DATA TABLE
5. a color-coded tier/state TABLE  6. a horizontal numbered PROCESS FLOW (the visual path)
7. a WITH/WITHOUT comparison  8. a 5-cell KPI strip  9. solid CTA block  10. footer.
Lesson: do not repeat ONE module type (a row list = tabular). Stack varied modules, each data-rich.
docs-05 Background Jobs rebuilt to this standard in Dark Ultron (DM Sans/Mono, book/kraft, NO green;
failed=book-dark, paused/without=cloud). File: docs-05-jobs-linkedin-v1. Supersedes journey + panel.

### CORRECTION (same day): keep the format, change only the requested element
Operator KEPT the JOURNEY format (hero hook + donut > entry-trigger band > FOCAL >
4-way outcome branch > artifact band > CTA). The v1 full-reformat to script-23 was
REJECTED: "I didn't tell you to change the format." Reinforces §25: change ONLY the
element asked for. The density technique above (sig-card: big number + 2-line desc +
mini data table + progress bar) was applied INSIDE the central focal panel only, as a
3x2 grid of step-kind child-job cards. Canonical: docs-05-jobs-linkedin-journey. v1 removed.
- docs-05 Background Jobs TikTok: infographic (1080x1450, hero donut + 6 horizontal step-cards with inline data + bars) and carousel (8 slides 1080x1920) reusing the docs-04 carousel CSS verbatim (cover > triggers > donut > mission/6 kinds > streams live > 5 states+controls > artifact lands > CTA JOBS). Dark Ultron, safe-zone respected.
- docs-05 TikTok carousel REBUILT with alternating per-slide layouts (operator: too similar to the last 10): cover > icon-rows (triggers) > big donut + states > flow nodes (mission) > big stat (124/200 + bar) > state-pill grid > with/without split > CTA. No two content slides share a layout. Caption uses the new 15.6 social format (one beat per paragraph) + the fixed 5-hashtag set.
- docs-05 TikTok carousel PAGINATION CONGRUENCE (operator: slides not aligned between them): fixed title reserve (min-height) so every slide's title ends at the same Y and content starts at the same Y (measured 0px spread, slides 2-7; cover+CTA title also start at Y=239). Sparse slides filled with substance (triggers got example chips), heroes (donut/stat) centred, lists (flow/pills/split) fill. Jargon 'Hitl' renamed to 'Approval'. Measure script: /tmp/measure_congruence.py.
- TikTok/IG vertical SAFE ZONE (operator: top margin not respected, mast got covered): top inset is **250px** (not 150), right 130, bottom 330, left 70. Memorized in CLAUDE.md 9. Pre-flight: mast top >= 250px. docs-05 carousel re-rendered with top:250.
- docs-06 Brain LinkedIn: reuses the approved docs-05 journey skeleton (hero + visual > 'it goes in' write-paths band > FOCAL > 'it comes back' band > CTA). Focal = 2x2 grid of the 4 surfaces (Notes/Memories/Files/Agreements), each a dense card with icon + name + tag + 5-row data table (written by / editable / in chat / detail). Retrieval band = 3 signal cards (semantic / tag+agent / recency). Cards 16.4% empty. Keyword BRAIN.
