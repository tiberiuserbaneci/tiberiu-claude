#!/usr/bin/env python3
# THE TOP FLOOR IS EMPTY - adaptare IG Scraped (s3src23) in context Ultron (layered AI stack angle)
import importlib.util
spec=importlib.util.spec_from_file_location("T2","/home/user/tiberiu-claude/content/_hitl-src/build_team_3d.py")
T2=importlib.util.module_from_spec(spec); spec.loader.exec_module(T2)
wo=lambda s:(s,T2.WHITE); co=lambda s:(s,T2.CORAL); mu=lambda s:(s,T2.MUTED)
M="/home/user/tiberiu-claude/content/_hitl-src/models_clay/s3src23"; LIB=T2.LIB
PREMIUM=1
TITLE="THE TOP FLOOR IS EMPTY"
T2.ACCENT=(212, 162, 127); T2.CORAL=T2.ACCENT
T2.OBJ_THR=30
T2.COVER=dict(head=[[wo('You stacked the models.')],[co('Nobody runs the top.')]])
T2.CONTENT=[
 ('THE BEDROCK', [[wo('Models are the floor,')],[co('not the building.')]], 'Claude, GPT, Gemini, Grok. Raw intelligence you rent by the token, swappable under everything.', [co('The floor, not the stack.')], f"{M}/floor.png", 1.0),
 ('THE INTAKE', [[wo('Knowledge flows up,')],[co('not sideways.')]], 'The web, your docs, every file collapse into one brief the layers above can act on.', [co('Read once, reused.')], f"{M}/inflow.png", 1.0),
 ('THE AGENT LAYER', [[wo('Seven hands,')],[co('one crew.')]], 'CORTEX, SPECTER, STRIKER, PULSE, SENTINEL, AMPLIFY, COUNSEL. Each owns a single job.', [co('Named, not generic.')], f"{M}/agents.png", 1.0),
 ('THE GAP', [[wo('The top floor')],[co('sits empty.')]], 'You wired models, search and agents. The floor that runs them is the one nobody installs.', [co('A stack with no roof.')], f"{M}/gap.png", 1.0),
 ('THE ROUTER', [[wo('One dispatcher')],[co('over every model.')]], 'It reads the job, hires the agent, picks the tier. Cents for a lookup, a bit more for hard calls.', [co('You never pick a model.')], f"{M}/router.png", 1.0),
 ('THE SPINE', [[wo('One memory')],[co('under the whole stack.')]], 'ICP, pipeline, pricing, docs. Every floor draws the same core, so nothing forgets you.', [co('One core, every floor.')], f"{M}/spine.png", 1.0),
 ('THE GATE', [[wo('Nothing leaves')],[co('without your tap.')]], 'Every external move parks at the top floor for your approval. Augmented, never unsupervised.', [co('Still your company.')], f"{M}/gate.png", 1.0),
 ('THE OPERATOR LAYER', [[wo('Own the floor')],[co('that runs the rest.')]], 'Foundation, research, agents, memory, gate. One operator layer seated on top of all of it.', [co('The top floor, yours.')], f"{M}/toptier.png", 1.0),
]
T2.CTA=("SAVE THIS", [[wo('Want the full stack map?')],[co('comment OPERATOR.')]], "/home/user/tiberiu-claude/content/_hitl-src/_templates/cta/cta-operator.png", 0.82)
T2.CLOSE=dict(l1='Save this and claim',l2='the top floor.',q='Which floor of your stack is still empty?')
MARK2="claude"
