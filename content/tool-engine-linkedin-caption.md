# The routing engine - LinkedIn caption kit
For: tool-engine-linkedin | Keyword: ENGINE | Ultron brand
Applies the best-performer formula (post 7473683676527071232: hidden-mechanic "Your AI..." hook + comment CTA; 35 comments). Topic = the next layer: tool routing.

## CAPTION

Your prompt does not go straight to an answer. It gets routed twice before your AI does anything.

Most people picture an AI assistant as one model in a chat box. I used to think the model was the whole product too. The work that decides whether it is actually useful happens before a single word comes back, in how the request gets routed.

- - -

I watched what Ultron does the moment you ask it for something, and it is two routing hops, not one.

First it routes to a model. The router reads the job and picks the tier: a quick lookup goes to Haiku, the default work to Sonnet, the hard judgement to Opus. You never choose, it chooses.

Then it routes to a tool. This is the part most AI skips entirely.

→ The dispatcher takes your request and finds the exact tool, one of 92, by name. It tries the module first, falls back to the native catalog, and resolves in constant time. No fumbling through a list.

→ It runs that tool behind a connection gate, so it only ever fires the apps you have actually connected. It never reaches into something you do not use.

→ Anything irreversible, a send, a charge, a post, stops at an approval gate and waits for your yes.

→ New tools plug in as self-contained modules. 84 native plus 7 for Stripe plus 1 for approvals is 92 today, and the next one ships without touching the tools that already work, so the catalog only ever grows.

- - -

Nobody posts about this part because it is invisible. But it is the whole difference between an AI that talks and an AI that does the job. The chat is the surface. The routing engine underneath, model then tool then gate, is the product. An AI with no tools, or one that grabs the wrong tool, hands you a confident paragraph instead of a finished task.

Most teams still judge AI by the answer in the box. The leverage is one layer down, in how the request gets routed.

- - -

Follow for one AI system for founders every day.
Comment ENGINE and I will send you the map of how your AI routes a request to the right tool.

## ALT TEXT

Dark editorial infographic on a slate background with book-orange accents and the Ultron logo. The headline reads 'Your AI picks a model. The hard part is picking the right tool,' with the right tool in book-orange. A four-cell stat strip shows 92 native tools, 2 routing hops, module first then native, and 1 approval gate. Below sits a four-stage routing pipeline: step one route to the model, with chips Lite equals Haiku, Smart equals Sonnet, Deep equals Opus; step two route to the right tool, with chips module-first, native fallback, and resolve by name; step three run it behind a connection gate; step four ship it through a human-in-the-loop approval gate. A dashed strip totals 84 native plus 7 Stripe plus 1 approval equals 92 and notes new tools plug in as modules. Closes with a Comment ENGINE call to action and the Ultron logo.

## FIRST COMMENT

Two routing hops happen before your AI says a word: it picks the model tier, then a dispatcher finds the exact tool out of 92 and runs it behind an approval gate. The answer in the chat box is the last step, not the system.

Comment ENGINE and I will send you the map of how the request actually routes to the right tool.
