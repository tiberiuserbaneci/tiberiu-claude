# Claude Code in 2 minutes - TikTok caption kit
For: claude-code-start-editorial45-infographic (+ carousel) | Keyword: CODE

## CAPTION

Comment CODE and I will send you a starter CLAUDE.md and the setup.

Claude Code turns your terminal into a coding agent. Most people never get past install.

Here are the only 5 moves you need.

Run claude in any project folder.

Type /init so it reads your repo and writes a CLAUDE.md memory.

Hit shift tab for plan mode, so it thinks before it writes a single line.

Let it run. It edits files, runs tests and fixes its own errors while you approve.

Type /resume to pick up any past session with full context.

That is the whole loop, and it ships features while you sleep.

#claude #ai #founder #startup #buildinpublic

## ALT TEXT

Light editorial infographic on a cream background with coral accents and a Claude sunburst mark. The title reads Claude Code in 2 minutes. Five numbered rows each pair a dark terminal command chip with a plain explanation: claude to start it in any project, /init to give it memory through a CLAUDE.md, shift tab for plan mode so it plans before code, let it run so it edits and tests and fixes its own errors, and /resume to pick up a past session with full context. Closes on a Comment CODE call to action with the Ultron logo and 51ultron.com.

## FIRST COMMENT

The trick is not typing more, it is /init and plan mode. Give it memory and let it plan, and it stops guessing.

Comment CODE and I will send you a starter CLAUDE.md plus the setup steps. More at 51ultron.com
